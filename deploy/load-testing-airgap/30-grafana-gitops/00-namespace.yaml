# Grafana GitOps 워크플로용 namespace.
# 운영에선 bitbucket 을 source-of-truth 로 쓰지만, testbed/local 에선 gitea
# (GitLab 호환 git HTTP/API surface) 를 띄워 동일한 GitOps 흐름을 재현한다.
apiVersion: v1
kind: Namespace
metadata:
  name: gitops
  labels:
    purpose: grafana-gitops
